/**
 * @file Test Helper Functions
 * @version 1.0.0
 * @description Helper functions for testing the MCP Proxy Wrapper
 */

/**
 * Creates a standardized tool result object
 * 
 * @param data - The data to include in the result
 * @returns A formatted tool result object
 */
export const createToolResult = (data: any): any => {
  return data;
}; 